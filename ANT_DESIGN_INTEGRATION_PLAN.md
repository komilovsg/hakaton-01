# План интеграции Ant Design в проект

## 📦 Установка
```bash
yarn add antd
```

## 🎯 Компоненты для замены/улучшения

### 1. **Таблицы (Table)** ⭐ ПРИОРИТЕТ ВЫСОКИЙ

#### 1.1 `ChannelsTable.tsx`
- **Текущее состояние**: Обычная HTML таблица
- **Замена на**: `antd Table`
- **Преимущества**: 
  - Сортировка по колонкам
  - Фильтрация
  - Пагинация
  - Адаптивность
  - Встроенные действия (edit/delete)
- **Дополнительно**: 
  - `Tag` для статусов (normal/high-loss/critical)
  - `Popconfirm` для подтверждения удаления

#### 1.2 `OfficialData.tsx` - Таблица 16 (обе части)
- **Текущее состояние**: HTML таблицы
- **Замена на**: `antd Table`
- **Особенности**: 
  - Сложная структура с объединенными ячейками (rowSpan/colSpan)
  - Статичные данные для отображения

#### 1.3 `OfficialData.tsx` - Таблица 14 (редактируемая)
- **Текущее состояние**: HTML таблица с input полями
- **Замена на**: `antd Table` с editable cells
- **Особенности**:
  - Редактируемые ячейки через `Form.Item`
  - Валидация данных
  - Автоматическое заполнение первой строки из таблицы 16

### 2. **Формы (Form)** ⭐ ПРИОРИТЕТ ВЫСОКИЙ

#### 2.1 `ChannelForm.tsx`
- **Текущее состояние**: Кастомная форма с overlay
- **Замена на**: `antd Form` + `Modal`
- **Компоненты**:
  - `Modal` вместо overlay
  - `Form` с `Form.Item`
  - `InputNumber` для числовых полей
  - `Select` для статуса
  - `Button` для действий
- **Валидация**: Встроенная валидация Ant Design

#### 2.2 `WaterLossCalculator.tsx`
- **Текущее состояние**: Кастомные input поля
- **Улучшение**: `antd Form` + `InputNumber`
- **Дополнительно**:
  - `Card` для группировки секций
  - `Divider` для разделения
  - `Statistic` для отображения результатов

### 3. **Уведомления (Message)** ⭐ ПРИОРИТЕТ ВЫСОКИЙ

#### 3.1 `Landing.tsx`
- **Места использования**:
  - При добавлении канала в таблицу: `message.success('Канал добавлен в таблицу')`
  - При сохранении изменений: `message.success('Данные канала сохранены')`
  - При удалении: `message.success('Канал удален из таблицы')`

#### 3.2 `AIAnalysis.tsx`
- **Места использования**:
  - Успешный анализ: `message.success('Анализ завершен')`
  - Ошибка анализа: `message.error('Ошибка при анализе')`
  - Загрузка: `message.loading('Анализ выполняется...')`

#### 3.3 `OfficialData.tsx`
- **Места использования**:
  - При сохранении данных таблицы 14: `message.success('Данные сохранены')`
  - При изменении режима редактирования: `message.info('Режим редактирования включен')`

### 4. **Кнопки (Button)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 4.1 Все компоненты
- **Замена**: Все `<button>` на `antd Button`
- **Типы**:
  - `primary` - основные действия
  - `default` - вторичные действия
  - `danger` - удаление
  - `ghost` - прозрачные кнопки
- **Иконки**: `Button` с `icon` prop

### 5. **Подтверждения (Popconfirm)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 5.1 `ChannelsTable.tsx`
- **Использование**: Обернуть кнопку удаления в `Popconfirm`
- **Текст**: "Вы уверены, что хотите удалить этот канал?"

#### 5.2 `Landing.tsx`
- **Использование**: Подтверждение удаления канала из таблицы
- **Текст**: "Вы уверены, что хотите удалить этот канал из таблицы?"

### 6. **Загрузка (Spin, Skeleton)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 6.1 `AIAnalysis.tsx`
- **Текущее**: Кастомный spinner
- **Замена на**: `Spin` с `tip="Анализирую данные канала..."`
- **Дополнительно**: `Skeleton` для предзагрузки структуры результата

#### 6.2 `ChannelsTable.tsx`
- **Добавить**: `Skeleton` для состояния загрузки данных

#### 6.3 `Map.tsx`
- **Добавить**: `Spin` при загрузке карты

### 7. **Прогресс (Progress)** ⭐ ПРИОРИТЕТ НИЗКИЙ

#### 7.1 `AIAnalysis.tsx`
- **Использование**: Показать прогресс анализа (если API поддерживает)
- **Тип**: `Progress` с `percent`

#### 7.2 `WaterLossCalculator.tsx`
- **Использование**: Визуализация эффективности канала
- **Тип**: `Progress` для КПД (0-100%)

### 8. **Временная линия (Timeline)** ⭐ ПРИОРИТЕТ ВЫСОКИЙ

#### 8.1 `AIAnalysis.tsx`
- **Использование**: Показать историю изменений канала или этапы анализа
- **Структура**:
  - Этап 1: Сбор данных
  - Этап 2: Анализ потерь
  - Этап 3: Расчет эффективности
  - Этап 4: Генерация рекомендаций

#### 8.2 `ChannelInfo.tsx`
- **Использование**: История измерений канала (если есть данные)
- **Альтернатива**: История статусов (normal → high-loss → critical)

### 9. **Карточки (Card)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 9.1 `ChannelInfo.tsx`
- **Текущее**: Кастомные метрики
- **Улучшение**: `Card` с `Statistic` для каждой метрики
- **Преимущества**: Единый стиль, иконки, цвета

#### 9.2 `WaterLossCalculator.tsx`
- **Улучшение**: `Card` для группировки секций (параметры, результаты)

#### 9.3 `Landing.tsx` - Expected Result секция
- **Текущее**: Кастомные карточки
- **Замена на**: `Card` с `Card.Grid`

### 10. **Статистика (Statistic)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 10.1 `ChannelInfo.tsx`
- **Использование**: Отображение метрик (потери, КПД, расход)
- **Преимущества**: Красивое форматирование чисел, иконки, тренды

#### 10.2 `WaterLossCalculator.tsx`
- **Использование**: Результаты расчетов

### 11. **Теги (Tag)** ⭐ ПРИОРИТЕТ СРЕДНИЙ

#### 11.1 `ChannelsTable.tsx`
- **Текущее**: Кастомные badge для статусов
- **Замена на**: `Tag` с цветами:
  - `success` - normal
  - `warning` - high-loss
  - `error` - critical

#### 11.2 `ChannelInfo.tsx`
- **Замена**: Статус badge на `Tag`

### 12. **Модальные окна (Modal)** ⭐ ПРИОРИТЕТ ВЫСОКИЙ

#### 12.1 `ChannelForm.tsx`
- **Текущее**: Кастомный overlay
- **Замена на**: `Modal`
- **Преимущества**: 
  - Встроенная анимация
  - Лучшая доступность
  - Стандартное поведение

#### 12.2 `Landing.tsx` - Add to Table Prompt
- **Текущее**: Кастомный prompt
- **Замена на**: `Modal.confirm` или `Modal`

### 13. **Разделители (Divider)** ⭐ ПРИОРИТЕТ НИЗКИЙ

#### 13.1 `OfficialData.tsx`
- **Использование**: Разделение секций (данные, таблицы, формулы)

#### 13.2 `WaterLossCalculator.tsx`
- **Использование**: Разделение параметров и результатов

### 14. **Вкладки (Tabs)** ⭐ ПРИОРИТЕТ НИЗКИЙ

#### 14.1 `OfficialData.tsx`
- **Использование**: Разделить таблицы 16 и 14 на вкладки
- **Структура**:
  - Вкладка 1: Официальные данные
  - Вкладка 2: Таблица 16
  - Вкладка 3: Таблица 14 (редактируемая)

### 15. **Алерты (Alert)** ⭐ ПРИОРИТЕТ НИЗКИЙ

#### 15.1 `AIAnalysis.tsx`
- **Использование**: Показать ошибки вместо кастомного error-message
- **Тип**: `Alert` с `type="error"`

#### 15.2 `WaterLossCalculator.tsx`
- **Использование**: Предупреждения о некорректных данных

## 📋 Порядок внедрения

### Этап 1: Базовая настройка
1. Установить `antd`
2. Настроить провайдер `ConfigProvider` в `main.tsx`
3. Импортировать стили Ant Design

### Этап 2: Критичные компоненты
1. **Message** - уведомления (быстро, большой эффект)
2. **Table** - таблицы (основной функционал)
3. **Form + Modal** - формы редактирования
4. **Button** - все кнопки

### Этап 3: Улучшения UX
1. **Popconfirm** - подтверждения
2. **Spin/Skeleton** - загрузка
3. **Tag** - статусы
4. **Card + Statistic** - метрики

### Этап 4: Дополнительные фичи
1. **Timeline** - история/анализ
2. **Progress** - прогресс
3. **Alert** - предупреждения
4. **Tabs** - организация контента

## 🎨 Настройка темы

```typescript
// main.tsx
import { ConfigProvider } from 'antd';
import ruRU from 'antd/locale/ru_RU';

<ConfigProvider 
  locale={ruRU}
  theme={{
    token: {
      colorPrimary: '#0284c7', // Основной цвет проекта
      borderRadius: 8,
    },
  }}
>
  <App />
</ConfigProvider>
```

## 📝 Примечания

- Сохранить существующую функциональность
- Адаптировать под текущую цветовую схему
- Обеспечить поддержку i18n (ru/tj)
- Тестировать на всех экранах
- Постепенная миграция (не все сразу)

