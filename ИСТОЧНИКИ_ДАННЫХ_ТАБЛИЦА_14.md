# Источники данных для таблицы 14 (Давоми ҷадвали 14)

## Подтверждение: Данные берутся из таблицы 16

**Таблица 16:** "Ҷадвали 16. Масрафи каналҳои байни хоҷагҳо дар ПК 0 - и, АИО-ҳо ва магистралии ноҳияи Рудакӣ"

---

## Для месяцев Апрель, Май, Июнь

### Источник данных: `table16Part1Data`

Данные берутся из **первой части таблицы 16** (`table16Part1Data`), которая содержит данные для месяцев **Апрель, Май, Июнь**.

### Конкретные строки таблицы 16, используемые для расчета:

#### 1. Строка 1 таблицы 16: "Канали байни хоҷагӣ 1-МК дар ПК91+50" (number: 1)

**Данные для Апреля:**
- Декада I: `1061.3` л/с
- Декада II: `2028.5` л/с
- Декада III: `4788.9` л/с

**Данные для Мая:**
- Декада I: `7823.8` л/с
- Декада II: `14670.0` л/с
- Декада III: `16878.8` л/с

**Данные для Июня:**
- Декада I: `17699.6` л/с
- Декада II: `20047.0` л/с
- Декада III: `9583.1` л/с

#### 2. Строка 3 таблицы 16: "1-2К, АИО - 2" (number: 3)

**Данные для Апреля:**
- Декада I: `154.9` л/с
- Декада II: `296.1` л/с
- Декада III: `699.0` л/с

**Данные для Мая:**
- Декада I: `1142.0` л/с
- Декада II: `2141.3` л/с
- Декада III: `2463.7` л/с

**Данные для Июня:**
- Декада I: `2583.5` л/с
- Декада II: `2926.2` л/с
- Декада III: `1398.8` л/с

#### 3. Строка 4 таблицы 16: "1-3К, АИО - 3" (number: 4)

**Данные для Апреля:**
- Декада I: `57.6` л/с
- Декада II: `110.1` л/с
- Декада III: `259.8` л/с

**Данные для Мая:**
- Декада I: `424.5` л/с
- Декада II: `795.9` л/с
- Декада III: `915.8` л/с

**Данные для Июня:**
- Декада I: `960.3` л/с
- Декада II: `1087.7` л/с
- Декада III: `519.9` л/с

#### 4. Строка 5 таблицы 16: "1-4К, АИО - 4" (number: 5)

**Данные для Апреля:**
- Декада I: `40.6` л/с
- Декада II: `77.5` л/с
- Декада III: `183.1` л/с

**Данные для Мая:**
- Декада I: `299.1` л/с
- Декада II: `560.8` л/с
- Декада III: `645.2` л/с

**Данные для Июня:**
- Декада I: `676.6` л/с
- Декада II: `766.3` л/с
- Декада III: `366.3` л/с

---

## Как данные используются в расчетах таблицы 14

### Строка 1 таблицы 14: "1-1МК ПК90+50 ПК60+20"

**Формула:** `Qвх = Q(1-МК) + Q(1-4К)`

**Пример для Апреля, Декада I:**
- Q(1-МК) = `1061.3` л/с (из строки 1 таблицы 16)
- Q(1-4К) = `40.6` л/с (из строки 5 таблицы 16)
- **Qвх = 1061.3 + 40.6 = 1101.9** л/с

### Строка 2 таблицы 14: "1-1MK ПК60+20 – ПК00+00"

**Формула:** `Qх = Qfx строки 1 + Q(1-2К) + Q(1-3К)`

**Пример для Апреля, Декада I:**
- Qfx строки 1 = (рассчитанное значение из строки 1)
- Q(1-2К) = `154.9` л/с (из строки 3 таблицы 16)
- Q(1-3К) = `57.6` л/с (из строки 4 таблицы 16)
- **Qх = Qfx строки 1 + 154.9 + 57.6**

### Строка 3 таблицы 14: "1-1МК1 ПК134+70 – ПК74+40"

**Формула:** `Qх = Qfx строки 2 + Q(1-3К)`

Использует Q(1-3К) из строки 4 таблицы 16.

### Строка 4 таблицы 14: "1-1МК1 ПК74+40 – ПК00+00"

**Формула:** `Qх = Qfx строки 3`

Использует результат из строки 3.

---

## Код, подтверждающий источник данных

### В функции `calculateHydrologyTable`:

```typescript
// Для Апреля, Мая, Июня используется table16Part1Data
const isPart1 = month === 'apr' || month === 'may' || month === 'jun';
const row1_MK = isPart1 ? row1_MK_part1 : row1_MK_part2;  // Строка 1 таблицы 16
const row1_4K = isPart1 ? row1_4K_part1 : row1_4K_part2;  // Строка 5 таблицы 16
const row1_2K = isPart1 ? row1_2K_part1 : row1_2K_part2;  // Строка 3 таблицы 16
const row1_3K = isPart1 ? row1_3K_part1 : row1_3K_part2;  // Строка 4 таблицы 16

// Получаем значения Q из таблицы 16 для текущей декады
const Q_1_MK = row1_MK.decades[month][decadeIdx];  // Из строки 1
const Q_1_4K = row1_4K.decades[month][decadeIdx];  // Из строки 5
const Q_1_2K = row1_2K.decades[month][decadeIdx];  // Из строки 3
const Q_1_3K = row1_3K.decades[month][decadeIdx];  // Из строки 4
```

### Инициализация источников данных:

```typescript
// Получаем данные из таблицы 16 (Part1 для Апрель-Июнь)
const row1_MK_part1 = inputDataPart1.find(row => row.number === 1); // Строка 1: "Канали байни хоҷагӣ 1-МК"
const row1_4K_part1 = inputDataPart1.find(row => row.number === 5); // Строка 5: "1-4К, АИО - 4"
const row1_2K_part1 = inputDataPart1.find(row => row.number === 3); // Строка 3: "1-2К, АИО - 2"
const row1_3K_part1 = inputDataPart1.find(row => row.number === 4); // Строка 4: "1-3К, АИО - 3"
```

---

## Подтверждение

✅ **ДА, все правильно!**

Данные для таблицы 14 (Давоми ҷадвали 14) для месяцев **Апрель, Май, Июнь** берутся напрямую из **таблицы 16** (Ҷадвали 16), конкретно из:

- **`table16Part1Data`** - первая часть таблицы 16, которая содержит данные для Апреля, Мая и Июня
- Используются строки с номерами: **1, 3, 4, 5** из таблицы 16
- Данные берутся из поля `decades` для каждого месяца и каждой декады

Функция `calculateHydrologyTable` получает эти данные через параметры `inputDataPart1` и `inputDataPart2`, которые передаются из `table16Part1Data` и `table16Part2Data` соответственно.

